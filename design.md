# تصميم واجهة MNN Local AI

## هدف التجربة

يقدّم **MNN Local AI** تجربة محادثة عربية بسيطة تعمل محلياً على الهاتف بعد تنزيل النموذج. تعتمد الواجهة على وضع رأسي بنسبة 9:16، مع مواضع لمس رئيسية في النصف السفلي من الشاشة، وحالات واضحة تمنع المستخدم من محاولة المحادثة قبل جاهزية النموذج.

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف |
|---|---|---|
| شاشة الإعداد الأول | بطاقة تعريف مختصرة، حجم النموذج المتوقع، زر تنزيل النموذج | بدء التنزيل، عرض متطلبات المساحة، الانتقال التلقائي عند الجاهزية |
| شاشة التنزيل والتحقق | نسبة مئوية حقيقية، السرعة، الحجم المنزّل، حالة الاستئناف والتحقق | إيقاف التنزيل واستئنافه، عرض فشل التحقق وإعادة المحاولة |
| شاشة الدردشة | عنوان حالة النموذج، قائمة رسائل، حقل كتابة ثابت وزر إرسال/إيقاف | إرسال طلب، استقبال النص المتدفق، إيقاف التوليد، نسخ الرد |
| شاشة التفاصيل التقنية | حالة النموذج، مسار التخزين، قيمة SHA-256، آخر توقيت تحميل وقياسات الجاهزية | حذف النموذج محلياً، إعادة التحقق، الاطلاع على قياسات زمن الاستجابة |

## تخطيط الشاشات

تستخدم شاشة الإعداد الأول صفحة هادئة بمساحة علوية واسعة وعلامة تطبيق في المنتصف، تليها بطاقة توضح أن الاستدلال يتم محلياً. يوضع زر **تنزيل النموذج** بعرض كامل فوق المنطقة الآمنة السفلية لتمكين الاستخدام بيد واحدة. لا تُعرض أرقام سرعة أو حجم افتراضية؛ تظهر القياسات فقط عند توفرها من عملية التنزيل الفعلية.

تستخدم شاشة الدردشة شريط حالة قصيراً في الأعلى يعرض «جاهز محلياً» أو حالة التحميل الدافئ. تشغل الرسائل معظم المساحة، وتظهر رسائل المستخدم بمحاذاة اليمين ولون أزرق داكن، ورسائل النموذج بمحاذاة اليسار ضمن سطح محايد. يبقى مربع الإدخال في الأسفل مع زر إرسال دائري كبير. أثناء التوليد يتحول الزر إلى إجراء إيقاف واضح يمكن الوصول إليه بالإبهام.

تعرض شاشة التفاصيل التقنية المعلومات بطريقة قائمة إعدادات أصلية: صفوف قصيرة، عناوين ثانوية، وأفعال قد تغير البيانات مثل حذف النموذج داخل زر مميز بلون تحذيري مع نافذة تأكيد.

## تدفقات المستخدم الأساسية

1. يفتح المستخدم التطبيق، فتفحص شاشة الإعداد وجود ملف النموذج وصحته.
2. عند عدم وجود نموذج صالح، يضغط المستخدم زر التنزيل، ويرى التقدم الفعلي مع دعم الاستئناف بعد الانقطاع.
3. بعد اكتمال التنزيل، يتحقق التطبيق من SHA-256، ثم يحمّل الأوزان وينفذ warm-up قصيراً قبل أن يعرض شاشة الدردشة بحالة «جاهز محلياً».
4. يكتب المستخدم رسالة ثم يضغط إرسال. تظهر فقاعة المستخدم فوراً وتُلحق استجابة النموذج تدريجياً بالرسالة ذاتها.
5. أثناء التوليد يمكن للمستخدم الضغط على إيقاف؛ تتغير الحالة إلى «تم الإيقاف» ولا تُنشأ رسالة جديدة غير مقصودة.
6. من التفاصيل التقنية يستطيع المستخدم التحقق من الحالة أو حذف النموذج والعودة إلى تدفق التنزيل.

## خيارات الألوان

| الغرض | اللون الفاتح | اللون الداكن | المبرر |
|---|---|---|---|
| اللون الأساسي | `#0B5FFF` | `#7AA2FF` | أزرق تقني هادئ يميز الأفعال الرئيسية ورسائل المستخدم |
| الخلفية | `#F7F9FC` | `#0B1020` | تباين منخفض التشتيت للمحادثة الطويلة |
| السطح | `#FFFFFF` | `#151C2F` | فصل بسيط للبطاقات والرسائل |
| النص | `#152033` | `#F2F5FA` | وضوح مرتفع للنص العربي |
| الناجح | `#16794B` | `#4ED190` | جاهزية النموذج واكتمال التحقق |
| التحذير | `#B45309` | `#F6B85D` | تنزيل معلّق أو استئناف مطلوب |
| الخطأ | `#C62828` | `#FF8A8A` | فشل التحقق أو تعذر التحميل |

## مبادئ إمكانية الوصول

تعتمد جميع الأزرار على أهداف لمس لا تقل عن 44 نقطة، وتقرن الأيقونات بنصوص أو تسميات وصول. لا تعتمد حالات التنزيل أو الخطأ على اللون وحده، بل تعرض وصفاً نصياً واضحاً. يُضبط النص العربي بمحاذاة مناسبة واتجاه RTL، مع إبقاء معلومات الملفات التقنية قابلة للقراءة بخط أحادي المسافة عند الحاجة.
